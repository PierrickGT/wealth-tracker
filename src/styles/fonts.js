import { rem } from 'polished';
import { css } from 'styled-components';

/**
 * Font families
 * @type {Object}
 */
export const family = {
    montserrat: 'Montserrat'
};

/**
 * Font sizes
 * @type {Object}
 */
export const size = {
    big: css`${rem(22)}`
};
